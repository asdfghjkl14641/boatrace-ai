# 環境変数・接続情報 (絶対にGitにアップしない)
.env
.env.local
*.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.pytest_cache/

# ログファイル
logs/
*.log

# 一時データ (取得したCSVなど、必要なら残す)
data/
*.csv

# OS / エディタ
.DS_Store
Thumbs.db
.vscode/
.idea/

# SQLite ローカルDB (サイズ大のため通常は Git に含めない。
# 必要ならLFSで管理するか、明示的に削除)
boatrace.db
boatrace.db-journal
boatrace.db-wal
boatrace.db-shm

# DB バックアップ・テスト DB (巨大)
boatrace_backup_*.db
test_artifact*.db
daily_*.db

# GH Actions artifact DB (各 30-400MB)
bulk_db/
bulk_db_kv2/
phase2_db/
sampling_db/
sampling_db_kv2/

# 進捗 JSON (GH Actions では空から開始すべき)
scripts/*_progress.json
scripts/*_progress_*.json
